{
  "name": "handoff-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Short-URL service for portable AI agent context handoffs (Cloudflare Worker + KV)",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create HANDOFFS",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
